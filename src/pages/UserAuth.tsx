import styled from 'styled-components';

function UserAuth() {
  return <UserAuthtContainer className='container'>UserAuth</UserAuthtContainer>;
}
const UserAuthtContainer = styled.div``;

export default UserAuth;
